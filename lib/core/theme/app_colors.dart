import 'package:flutter/material.dart';

class AppColors {
  // Cores base
  static const Color scaffoldBackground = Color(0xff191524);
  static const Color inputBackground = Color(0xff2B2C4E);
  static const Color white = Colors.white;
  static const Color white60 = Colors.white60;
  static const Color lightText = Color(0xffFAFAFC);
  static const Color purplePrimary = Color(0xff7C3AED);

  // Cores semânticas (opcional)
  static const Color primary = purplePrimary;
  static const Color secondary = inputBackground;
}